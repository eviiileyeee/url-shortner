<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }
    </style>
</head>
<body>
    <div>
        <h1> URL SHORTNER</h1>
        <% if (locals.id) { %>
              <p>
                URL Generated: http://localhost:8001/<%=
                id
                     %>
              </p>
        <% } %>
        <form method="post" action="/url">
            <label for="">Enter your Current URL</label>
            <input type="text" name="url" id="" placeholder="https://example.com">
            <button type="submit">Generate</button>
        </form>
    </div>
    <div>
        <% if (locals.urls) { %>
         <table>
            <thead>
                <th> S. no.</th>
                <th> Short ID</th>
                <th> Redirect URL</th>
                <th> Click count</th>
            </thead>
            <tbody>
                <% urls.forEach((url,index)=> { %>
                    <tr>
                        <td><%= index + 1 %></td>
                        <td><%= url.shortId %></td>
                        <td> <%= url.redirectUrl %></td>
                        <td> <%= url.visitHistory.length %></td>
                     </tr>
                <% }) %>
            </tbody>
         </table>
        <% } %>
    </div>
</body>
</html>